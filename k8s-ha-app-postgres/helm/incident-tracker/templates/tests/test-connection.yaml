apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "incident-tracker.fullname" . }}-test-connection"
  labels:
    {{- include "incident-tracker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "incident-tracker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
