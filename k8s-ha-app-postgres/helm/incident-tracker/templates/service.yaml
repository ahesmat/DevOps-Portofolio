apiVersion: v1
kind: Service
metadata:
  name: {{ include "incident-tracker.fullname" . }}
  labels:
    {{- include "incident-tracker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "incident-tracker.selectorLabels" . | nindent 4 }}
